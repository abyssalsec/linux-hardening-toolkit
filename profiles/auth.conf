name=auth
description=PAM and password authentication security baseline
enabled_checks=auth.pam-stack,auth.null-passwords,auth.password-quality,auth.password-min-length,auth.password-history,auth.failed-login-lockout,auth.lockout-policy,auth.password-hashing
