name=updates
description=Automatic security update and package maintenance baseline
enabled_checks=updates.backend,updates.automatic-tool,updates.metadata-refresh,updates.auto-install,updates.security-scope,updates.scheduler,updates.reboot-required
