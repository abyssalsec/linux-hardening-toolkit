name=audit
description=Linux audit subsystem and security logging baseline
enabled_checks=logging.journald,logging.persistence,logging.syslog,logging.permissions,auditd.available,auditd.active,auditd.kernel-status,auditd.rules,auditd.critical-paths,auditd.config
