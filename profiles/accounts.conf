name=accounts
description=Local Linux account and password metadata security baseline
enabled_checks=accounts.local-database,accounts.login-defs-defaults,accounts.uid0-exclusive,accounts.duplicate-uids,accounts.passwd-shadowing,accounts.system-shells,accounts.shadow-access,accounts.empty-passwords,accounts.password-aging-consistency,accounts.password-change-dates
